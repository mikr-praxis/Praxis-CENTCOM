# CentCom Session Context — 2026-04-08

## What Was Done
- Updated GCP service account key for Praxis CentCom Google Calendar integration
  - Navigated to GCP Console, found praxis-centcom-calendar service account (no existing keys)
  - Created new JSON key (unified-atom-492422-n5-a11c5d844ef3.json)
  - Base64-encoded the key
  - Added `GOOGLE_SERVICE_ACCOUNT_KEY` as new env var in Vercel (mscott-8907s-projects/praxis-centcom)
  - Triggered redeployment
- Created `/sessionstart` skill at `.claude/skills/sessionstart/SKILL.md`
  - 10-step bootstrap: pull repo, read docs, read .context/ history, scan git, connect Chrome, verify MCP connectors, scan architecture, check branches, detect new env vars/integrations, synthesize report
  - Also handles end-of-session context saving ("wrap up" / "save context")
  - Tested all steps — everything works except git pull (this session had a stuck rebase, won't affect fresh sessions)
- Seeded the `.context/` directory for cross-session knowledge persistence

## Decisions Made
- `GOOGLE_SERVICE_ACCOUNT_KEY` lives in Vercel env vars (not in CentCom's /config DB) — the Google Calendar module on the config page shows "Env only" for some vars
- Context summaries go in `.context/` at project root, committed to the repo so they're versioned and available across machines
- `/sessionstart` is a **skill** (invoked with `/sessionstart`), NOT a scheduled task — the user rejected the scheduled task approach in favor of a manual command
- Skills are shared via the repo — anyone who clones Praxis-CENTCOM gets all skills. No plugin packaging needed.
- The system skills path (`mnt/.claude/skills/`) is read-only from the sandbox. Project skills live in `Praxis/.claude/skills/` which is writable.

## Active Work Streams
- Calendar integration: env var is set and deployed, but the ops calendar (ops@builtbypraxis.com) still needs to be shared with the service account email `praxis-centcom-calendar@unified-atom-492422-n5.iam.gserviceaccount.com` — Mikr needs to do this manually (it's a permissions change)
- `/sessionstart` is complete and tested

## Blockers / Issues
- Ops calendar sharing is a manual step — can't be done by Claude (permissions change)
- The `iam.disableServiceAccountKeyCreation` org policy may need an admin exception for future key rotations
- Git repo in this session was stuck in a rebase state with a locked index — will need `git rebase --abort` and lock file cleanup in a fresh session
- This session was spawned by a scheduled task, which blocked creating new scheduled tasks (Cowork guardrail against nested task creation)

## Key Files Modified
- `.claude/skills/sessionstart/SKILL.md` — the /sessionstart command skill
- `.context/2026-04-08.md` — this file, first context summary
- `.context/PENDING-sessionstart-task.md` — obsolete, can be deleted

## Environment & Tools Used
- Slack MCP: active, used to DM Mikr the service account key reminder and verified during /sessionstart test
- Gmail MCP: active, confirmed profile (mscott@builtbypraxis.com, 67 messages)
- Google Calendar MCP: active, listed calendars (primary + US Holidays, no ops calendar subscribed yet)
- Chrome: connected, used to navigate GCP Console, Vercel env vars, CentCom config page, Google Calendar settings
- Vercel dashboard: accessed via Chrome to add GOOGLE_SERVICE_ACCOUNT_KEY env var and trigger redeploy
- GCP Console: accessed via Chrome to view service account keys page

## App Architecture (as of this session)
- **Routes:** dashboard, tasks, comms, agents, events, budget + auth routes (sign-in, sign-up)
- **Lib modules:** anthropic, posthog, slack.ts, supabase, upstash, views
- **Components:** agents, budget, comms, dashboard, events, layout, tasks, ui, views
- **Env vars in code:** ANTHROPIC_API_KEY, NEXT_PUBLIC_POSTHOG_HOST, NEXT_PUBLIC_POSTHOG_KEY, NEXT_PUBLIC_SUPABASE_ANON_KEY, NEXT_PUBLIC_SUPABASE_URL, SLACK_BOT_TOKEN, SUPABASE_SERVICE_ROLE_KEY, UPSTASH_REDIS_REST_TOKEN, UPSTASH_REDIS_REST_URL, VERCEL_API_TOKEN, VERCEL_PROJECT_ID
- **Not in Vercel yet:** ANTHROPIC_API_KEY (shows "Not Configured" on CentCom /config page)

## Notes for Next Session
- **Run `/sessionstart` first** — the skill is at `.claude/skills/sessionstart/SKILL.md`
- Fix the git rebase state: `git rebase --abort`, remove any `.git/index.lock` file, then `git pull origin main`
- Confirm the ops calendar was shared with the service account — check if /calendar page in CentCom loads events
- The Vercel redeployment should be complete — verify praxis-centcom.vercel.app is healthy
- AGENTS.md warns this Next.js version has breaking changes — always read `node_modules/next/dist/docs/` before writing code
- CentCom config page shows 4 modules still not configured: AI Agents (Anthropic), Monday.com, ActiveCampaign, HubSpot
- ANTHROPIC_API_KEY is referenced in code but not set in Vercel — needed for the 9 AI agents
- Delete `.context/PENDING-sessionstart-task.md` — it's obsolete
- This session demonstrated the exact problem /sessionstart solves: lost context about file paths, access permissions, and tool availability
