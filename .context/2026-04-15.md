# CentCom Session Context — 2026-04-15

## Session Meta
- **Branch:** main (work done on claude/friendly-poincare worktree, merged via PRs #4, #5, #6)
- **Worktree:** claude/friendly-poincare (deleted at session end)
- **Started:** ~2026-04-15 18:30 UTC
- **Saved:** 2026-04-15 ~20:00 UTC
- **Trigger:** explicit /sessionend

## What Was Done
- Kicked off M1 of the Client Performance Dashboard build
- Verified all prerequisites: git identity, GitHub auth, MCP connectors (Slack, Gmail, Calendar, Chrome)
- Added `ANTHROPIC_API_KEY` to Vercel env vars (user did manually)
- Added `GOOGLE_SERVICE_ACCOUNT_EMAIL` to Vercel env vars (via Chrome automation)
- Built full M1 Foundation:
  - Supabase migration `008_client_performance.sql` — 4 tables (clients, data_sources, metric_snapshots, client_events) + indexes + Mashore seed
  - Metric type system: `lib/metrics/` with call, webinar, challenge funnel definitions (canonical metrics, aliases, benchmarks, stages)
  - Ingestion pipeline: `lib/ingest/` — Google Sheets API client (JWT auth), CSV parser, Anthropic AI mapper (claude-opus-4-6 with prompt caching)
  - API routes: `/api/ingest/sheets`, `/api/ingest/csv`, `/api/clients/[slug]/metrics` (GET + POST)
  - Dashboard UI: `/dashboard/[slug]` server component + client component with 4 views (dashboard, sheet ingest, CSV ingest, mapping review)
  - Components: KPICard, FunnelViz, ClientTrendChart, DataQualityBanner, MappingReviewUI
  - Updated `lib/supabase/types.ts` with Client, DataSourceRow, MetricSnapshotRow, ClientEventRow + Database table entries
- Fixed 3 rounds of Vercel build errors (TS strict type issues with Supabase literal unions + Recharts formatter types)
- Created `/clients` route with live build progress tracker (M1-M4 milestones, auto-detects what's configured)
- Added "Clients" tab to sidebar with BarChart3 icon, exec-only role permission
- Created `Client-Performance-Dashboard-Setup-Roadmap.docx` with step-by-step setup instructions
- Merged to main via PRs: #4 (M1 foundation), #5 (roadmap docx), #6 (Clients tab)
- All 3 production deploys went green

## Decisions Made
- **Mashore defaulted to `call` funnel** — user said "meeting for now", closest match. Update when confirmed.
- **`/clients` route for build progress, `/dashboard/[slug]` for actual dashboards** — keeps concerns separate
- **Client-facing pages deferred** — `/clients/[slug]` reserved for future public-facing dashboards with password/token auth
- **AI mapper uses Option A (human approval required)** — no auto-import of metric snapshots
- **Reusing Calendar service account for Sheets** — same GCP project, just need Sheets API enabled

## Active Work Streams
- Client Performance Dashboard is at M1 complete (code deployed) but infrastructure not yet wired:
  - Supabase migration not yet run (user needs admin permissions)
  - Google Sheets API not yet enabled on GCP (user needs admin permissions)
  - Mashore funnel type not confirmed
  - No data source connected yet

## Blockers / Issues
- **GCP permissions** — user can't enable Sheets API right now, needs admin access
- **Supabase SQL Editor** — user can't run migration right now, needs admin access
- **Chrome extension blocks GCP Console and Supabase dashboard** — had to provide manual instructions for both
- **No Node.js in sandbox** — couldn't run `tsc` locally, had to iterate via Vercel deploy errors (3 rounds)

## Key Files Modified
- `supabase/migrations/008_client_performance.sql` — 4 tables + Mashore seed
- `lib/metrics/` — types.ts, call-funnel.ts, webinar-funnel.ts, challenge-funnel.ts, index.ts
- `lib/ingest/` — sheets-client.ts, csv-parser.ts, metric-mapper.ts
- `app/api/ingest/sheets/route.ts` — Google Sheets ingestion endpoint
- `app/api/ingest/csv/route.ts` — CSV ingestion endpoint
- `app/api/clients/[slug]/metrics/route.ts` — Metric query + mapping approval endpoint
- `app/(app)/dashboard/[slug]/page.tsx` — Server component for client dashboard
- `app/(app)/dashboard/[slug]/client-dashboard.tsx` — Client component with ingestion flows
- `components/dashboard/client/*.tsx` — KPICard, FunnelViz, ClientTrendChart, DataQualityBanner, MappingReviewUI
- `app/(app)/clients/page.tsx` — Build progress page (server component)
- `app/(app)/clients/clients-build-progress.tsx` — Milestone tracker with live status checks
- `components/layout/Sidebar.tsx` — Added Clients nav item
- `lib/roles.ts` — Added /clients route permission (exec only)
- `lib/supabase/types.ts` — Added 4 new table types to Database
- `Client-Performance-Dashboard-Setup-Roadmap.docx` — Setup instructions document

## Environment & Tools Used
- **Slack MCP:** connected, used for channel search during bootstrap
- **Gmail MCP:** connected, confirmed mscott@builtbypraxis.com (124 messages)
- **Google Calendar MCP:** connected, listed calendars (primary + US Holidays)
- **Chrome:** connected, used to navigate Vercel env vars page (add GOOGLE_SERVICE_ACCOUNT_EMAIL), check deploy logs (3 rounds of error diagnosis), view deployment list
- **Vercel dashboard:** accessed via Chrome for env vars and deploy monitoring
- **GCP Console:** blocked by Chrome extension — gave user manual instructions
- **Supabase dashboard:** blocked by Chrome extension — gave user manual instructions

## Notes for Next Session
- **Run `/sessionstart` first** as always
- The `/clients` page on praxis-centcom.vercel.app shows exactly what's blocked — use it as the checklist
- **Three things need admin permissions before M2 can start:**
  1. Enable Sheets API: `console.cloud.google.com/apis/library/sheets.googleapis.com?project=unified-atom-492422-n5`
  2. Run Supabase migration: `supabase.com/dashboard/project/xnbsvfjkmpzoxrdhfpwx/sql/new` → paste `supabase/migrations/008_client_performance.sql`
  3. Confirm Mashore funnel type + get Sheet URL
- The roadmap docx has all details: `Client-Performance-Dashboard-Setup-Roadmap.docx`
- Vercel builds fail fast on TS errors since no local Node.js — be extra careful with Supabase typed queries (literal unions, not plain strings)
- Memory files updated: `feedback-fix-builds.md` (monitor deploys), `feedback-permissions.md` (work autonomously), `project-client-performance-dashboard.md` (build context)
- The `client-performance-dashboard` skill at `~/.claude/skills/client-performance-dashboard/SKILL.md` has the full spec — invoke it in future sessions touching this work

## Uncommitted Work
- None — all work merged to main via PRs #4, #5, #6. Working tree is clean.
